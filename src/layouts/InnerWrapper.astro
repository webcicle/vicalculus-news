---
---

<section>
	<slot />
</section>

<style>
	section {
		width: var(--max-width-uni);
		margin-inline: auto;
		padding-block: 1rem;
	}

	@media (min-width: 720px) {
		section {
			background-color: var(--color-bg);
			box-shadow: 2px 0px 5px rgba(0, 0, 0, 0.25),
				-2px 0px 5px rgba(0, 0, 0, 0.25);
		}
	}
</style>
